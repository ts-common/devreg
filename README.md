# NPM Local Install

## package.json

```json
{
  "scripts": {
    "li": "local-install"
  },
  "devDependencies": {
    "@ts-common/local-install": "^0.0.0"
  }
}
```

## Install local packages

```
npm run li
```